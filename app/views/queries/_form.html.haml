#ar
  = form_for new_query_path, method: 'GET' do
    = text_area_tag :sql_query, params[:sql_query]
    = submit_tag "Convert", name: nil

#sql
  = text_area_tag :ar_query, @query.sql_query, rows: 5

